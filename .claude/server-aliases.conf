# Maps shortcut aliases to canonical server names.
# Used by all guard hooks for SQL access control.
# Format: alias = server
# Case-insensitive matching on the alias side.

# SQL Server instances
SQL = localhost
