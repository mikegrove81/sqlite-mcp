# Global database rules - apply to ALL repos regardless of db-access.conf.
# Lines starting with # are comments. Blank lines ignored.

[database-deny]
# System databases - no repo should ever modify these via Claude.
# Output a script and run it yourself in SSMS if changes are needed.
master
msdb
model
tempdb
