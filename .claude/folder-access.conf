# Folder access overrides for this repo.
# Only list EXCEPTIONS to defaults.
#
# Defaults:
#   Own repo folder = write (implicit)
#   Other repo folders = read (implicit)
#   Network/system paths = deny (implicit)
#
# Format: path = read|write|deny
#
# Examples:
# C:\Deploy\MyApp = write
# C:\Repo\OtherProject\legacy = deny
