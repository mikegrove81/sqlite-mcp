# Formatting Rules

## Em-Dashes

Do not use em-dashes (—) in:
- Documents intended for distribution (SOPs, reports, exports)
- SQL scripts and objects (stored procedures, views, functions, triggers)

Use regular dashes (-) or rewrite the sentence instead.
